/*Na biblioteca "vetores.h" criar uma função 
com o protótipo void gerarVetorPositivoNegativo(int vetor[], int tam, int limNegativo,
int limPositivo) para gerar números positivos e 
negativos em um intervalo de valores. Use
vetor[i] = rand() % (limPositivo + limNegativo + 1) - limNegativo;
Usando essa função:
Gerar um vetor A de inteiros com 100 elementos, 
com valores entre -50 e +50. Armazenar em um vetor B
somente os valores positivos do vetor A.
Exemplo:*/